# On Hugging Face Spaces the gradio version actually installed comes from
# `sdk_version` in README.md front matter (currently 6.25.0, the version this
# backend is developed and tested against). The floor below only guards local
# installs from resolving an incompatible older major.
gradio>=6.0
pydantic>=2.7
pypinyin>=0.51
edge-tts>=6.1
mutagen>=1.47
moviepy>=2.0
numpy>=1.26
Pillow>=10.3
diffusers>=0.30
transformers>=4.42
torch>=2.3
accelerate>=0.31
google-generativeai>=0.7
spaces>=0.28
pytest>=8.2
